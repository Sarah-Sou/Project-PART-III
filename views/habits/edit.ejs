<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Edit Habit</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

<%- include("../partials/header") %>

<h1 class="page-title">Edit Habit</h1>

<form action="/habits/<%= habit._id %>/edit" method="POST" class="form-card">

  <label>Habit Title</label>
  <input type="text" name="title" value="<%= habit.title %>" required>

  <label>Description</label>
  <textarea name="description"><%= habit.description %></textarea>

  <label>Frequency</label>
  <select name="frequency">
    <option <%= habit.frequency === 'Daily' ? 'selected' : '' %>>Daily</option>
    <option <%= habit.frequency === 'Weekly' ? 'selected' : '' %>>Weekly</option>
    <option <%= habit.frequency === 'Monthly' ? 'selected' : '' %>>Monthly</option>
  </select>

  <label>Status</label>
  <select name="status">
    <option <%= habit.status === 'Active' ? 'selected' : '' %>>Active</option>
    <option <%= habit.status === 'Paused' ? 'selected' : '' %>>Paused</option>
  </select>

  <button class="submit-btn">Save Changes</button>
</form>

<%- include("../partials/footer") %>

</body>
</html>
